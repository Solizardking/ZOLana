# ZOLana — Tests (`test/`)

Test infrastructure for the Zcash/ZOLana node.

## Contents

| Directory | Description |
|-----------|-------------|
| `lint/` | Linting configuration and scripts (Python-based linters for code quality) |

For runtime tests, see `qa/` which contains RPC integration tests and Zcash-specific test suites.